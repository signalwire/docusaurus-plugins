{
  // CI uses this config to type-check ejected theme components; editors use it too.
  "extends": "@docusaurus/tsconfig",
  "compilerOptions": {
    "baseUrl": ".",
    "types": ["@signalwire/docusaurus-theme-llms-txt"]
  },
  "exclude": [".docusaurus", "build"]
}
